<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base theme used only to bootstrap the Activity. Compose controls the real look. -->
    <style name="Theme.ReplySense" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

</resources>

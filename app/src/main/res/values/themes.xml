<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- XML theme is only for Activity bootstrap. Compose controls the real UI. -->
    <style name="Theme.ReplySense" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

</resources>

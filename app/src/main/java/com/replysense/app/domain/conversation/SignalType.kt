package com.replysense.app.domain.conversation

/**
 * Canonical signal types surfaced to the UI.
 * Must remain stable across versions.
 */
enum class SignalType {
    UNCERTAINTY,
    ELEVATED_TONE,
    HIGH_ENGAGEMENT
}

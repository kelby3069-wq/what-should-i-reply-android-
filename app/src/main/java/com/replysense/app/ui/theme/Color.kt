package com.replysense.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * ReplySense palette:
 * - Primary: deep “ink” blue (premium, confident)
 * - Secondary: cool teal accent (modern, calm)
 * - Neutrals: slightly warm gray for surfaces (less sterile than pure gray)
 */

// ---------- Brand ----------
val Ink = Color(0xFF0B1220)         // near-black navy (premium “ink”)
val InkSoft = Color(0xFF101A2E)     // lifted ink
val Accent = Color(0xFF2C7DFA)      // modern blue (not default purple)
val Accent2 = Color(0xFF19C2A8)     // teal accent for chips/secondary actions
val Warning = Color(0xFFFFB020)
val Danger = Color(0xFFFF4D4F)
val Success = Color(0xFF2ECC71)

// ---------- Light neutrals ----------
val L_Bg = Color(0xFFF7F8FB)
val L_Surface = Color(0xFFFFFFFF)
val L_Surface2 = Color(0xFFF1F3F8)
val L_On = Color(0xFF0C1220)
val L_OnMuted = Color(0xFF5B6477)
val L_Border = Color(0xFFE3E7F0)

// ---------- Dark neutrals ----------
val D_Bg = Color(0xFF070A12)
val D_Surface = Color(0xFF0D1220)
val D_Surface2 = Color(0xFF121A2C)
val D_On = Color(0xFFEAF0FF)
val D_OnMuted = Color(0xFFB4BBD0)
val D_Border = Color(0xFF202944)

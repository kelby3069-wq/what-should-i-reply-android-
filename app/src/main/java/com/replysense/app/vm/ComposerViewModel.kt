package com.replysense.app.vm

import androidx.lifecycle.ViewModel

/**
 * Legacy compatibility ViewModel.
 *
 * The app is using AppViewModel now.
 * This file exists only to keep the project compiling while we stabilize CI.
 */
class ComposerViewModel : ViewModel() {
    // Intentionally empty.
}

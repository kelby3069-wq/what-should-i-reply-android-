package com.replysense.app.analytics

class AnalyticsTracker {

    fun track(event: AnalyticsEvent) {
        // Intentionally minimal & privacy-safe
        // Hook to Firebase / Amplitude later if desired
    }
}
